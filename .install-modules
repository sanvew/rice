#!/bin/sh

set -e

install-modules() {
    dotbot_bin=$1
    base_dir=$2
    shift 2
    modules=$@
    for config_path in $modules; do
        $dotbot_bin -d $base_dir -c $config_path -v
    done
}

install-modules $@
