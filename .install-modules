#!/bin/sh

set -e

install-modules() {
    dotbot_bin=$1
    base_dir=$2
    modules=$3
    shift 3
    for config_path in $modules; do
        $dotbot_bin -d $base_dir -c $config_path -v "$@"
    done
}

shift 3
install-modules "$1" "$2" "$3" "$@"
