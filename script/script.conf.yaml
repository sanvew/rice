- defaults:
    link:
      create: true
      relink: true
      backup: true

- link:
    ~/.local/bin:
      glob: true
      path: './script/**'
      exclude: [ '**/script.conf.yaml', '**/status-bar/**' ]
    ~/.local/bin/statusbar:
      glob: true
      path: './script/statusbar/*'
